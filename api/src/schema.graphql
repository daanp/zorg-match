scalar DateTime

type User {
    id: ID!
    username: String!
    appointments: [Appointment]
}

type Appointment {
    id: ID!
    start: DateTime!
    end: DateTime!
}

type Query {
    users: [User]
}

type Mutation {
    addUser(username: String): User
}
