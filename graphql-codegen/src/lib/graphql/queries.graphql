

fragment CareRequestItem on CareRequest{
    id
    clientName
    start
    end
    type
    status
}

query OpenCareRequests {
    careRequests(where: {status: OPEN}) {
        ...CareRequestItem
    }
}