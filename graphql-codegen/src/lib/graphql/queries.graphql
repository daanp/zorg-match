fragment AppointmentItem on Appointment {
    id
    start
    end
}

fragment UserItem on User{
    id
    username
    appointments {
      ...AppointmentItem
    }
}

query Users {
    users {
        ...UserItem
    }
}